name: Release
on:
  push:
    tags: ['v*']

permissions:
  contents: write

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            cmake \
            libwayland-dev libxkbcommon-dev xorg-dev \
            libgles2-mesa-dev libfreetype-dev
      - uses: mlugg/setup-zig@v2
      - name: Build portable binary
        run: |
          # zig cc doesn't report Debian/Ubuntu multiarch lib dirs to CMake, so
          # find_library can't locate OpenGL/X11/Freetype there. Point it at them.
          cmake --preset publish -DCMAKE_LIBRARY_PATH=/usr/lib/x86_64-linux-gnu
          cmake --build build/Publish
      - name: Publish draft release
        env:
          GH_TOKEN: ${{ github.token }}
          TAG: ${{ github.ref_name }}
        run: gh release create "$TAG" build/Publish/prock --draft --generate-notes
