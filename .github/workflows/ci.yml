name: CI
on:
  push:
    branches: [master]
  pull_request:

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            cmake clang clang-tidy \
            libwayland-dev libxkbcommon-dev xorg-dev \
            libgles2-mesa-dev libfreetype-dev \
            xvfb libgl1-mesa-dri
      - name: Configure
        run: cmake --preset debug
        env:
          CC: clang
          CXX: clang++
      - name: Build
        run: cmake --build build/Debug
      - name: Unit tests
        run: ./build/Debug/prock_tests
        timeout-minutes: 5
      - name: UI tests
        run: xvfb-run -a -s "-screen 0 1920x1080x24" ./build/Debug/prock_ui_tests
        timeout-minutes: 5
        env:
          LIBGL_ALWAYS_SOFTWARE: 1

  format:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: Install clang-format
        run: |
          sudo apt-get update
          sudo apt-get install -y clang-format
      - name: Check formatting
        run: find src -name '*.cpp' -o -name '*.h' | xargs clang-format --dry-run --Werror
